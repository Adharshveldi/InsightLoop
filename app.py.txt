import streamlit as st
import pandas as pd

st.title("🚗 Car Price BI Assistant")

uploaded_file = st.file_uploader("Upload your car dataset", type="csv")

if uploaded_file:
    df = pd.read_csv(uploaded_file)
    st.write("Here's a preview of your data:")
    st.dataframe(df)
